#lang racket

(provide (all-defined-out))

(define context-definitions-empty
  (hash))

;; TODO
(define (apply-context-to-filter-expression filter-expression)
  filter-expression)

;; TODO
(define (apply-context-to-modify-expression modify-expression)
  modify-expression)
